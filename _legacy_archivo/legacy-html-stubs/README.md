# Legacy HTML stubs (no servir como producto)

Residuos movidos fuera de `public/` para que **no** reemplacen la SPA React.

- `homepage-instagram.html` — no es el home del producto
- `perfil-instagram.html` / `perfil.html` — stubs viejos

El login canónico es `src/pages/AuthPageUnificada.jsx` (ZONA PRO).
