# Legacy HTML stubs (NO usar en runtime)

Estos archivos son residuos de diseños antiguos (~10 meses / experimentos Instagram).

**No deben servirse como rutas del producto.**

- `homepage-instagram.html` — home HTML viejo (rompe el SPA React)
- `perfil-instagram.html` / `perfil.html` — stubs sin `#root`

El login canónico es React: `AuthPageUnificada` (marca **ZONA PRO**).
El home canónico es React: `HomePage` en `/` y `/home`.
