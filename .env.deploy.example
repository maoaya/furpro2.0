# Copia este archivo a .env.deploy y rellena con tus valores
# Nunca subas .env.deploy al repo (está ignorado en .gitignore si lo añadimos)

# Token personal de Netlify (User Settings > Applications > Personal access tokens)
NETLIFY_AUTH_TOKEN=

# Site ID del sitio de producción (Dashboard > Site settings > Site information)
NETLIFY_SITE_ID=

# Auto-confirmar el despliegue en el script
FUTPRO_DEPLOY_Y=1
