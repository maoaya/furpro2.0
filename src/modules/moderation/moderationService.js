// import db from '../../config/db';

export async function reportContent() {
  // await db('reportes').insert({ contentId, reason, userId });
}

export async function getReports() {
  // return db('reportes').select('*');
}
