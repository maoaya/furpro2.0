// Stubs para AuthCallback.jsx
export async function handleCallback(navigate) {
  // Implementación real: procesa callback OAuth
  console.log('[STUB] Procesando callback OAuth');
  if (navigate) navigate('/perfil-card');
}
