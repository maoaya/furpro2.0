// Stubs para MenuHamburguesa.jsx
export function onAccion(accion, navigate) {
  // Implementación real: navegación menú
  console.log(`[STUB] Acción menú: ${accion}`);
  if (navigate) navigate('/' + accion);
}
