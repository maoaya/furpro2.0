# Variables de entorno para entorno de test FutPro
NODE_ENV=test
# Activa fallbacks de rutas de test (por defecto activados si no se define)
FUTPRO_TEST_FALLBACKS=true
# Fuerza desactivación de fallbacks aunque sea entorno test
# FUTPRO_DISABLE_TEST_FALLBACKS=true

# Claves push opcionales (no necesarias en test)
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
